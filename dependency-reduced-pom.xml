<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.horrorcore</groupId>
  <artifactId>digimon-simulator</artifactId>
  <version>1.0.0</version>
  <build>
    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version>
        <configuration>
          <release>17</release>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-maven-plugin</artifactId>
        <version>${javafx.maven.plugin.version}</version>
        <configuration>
          <mainClass>${main.class}</mainClass>
          <jlinkImageName>digimon-simulator</jlinkImageName>
          <launcher>launcher</launcher>
          <mainClass>${main.class}</mainClass>
        </configuration>
      </plugin>
      <plugin>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.2.4</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals>
              <goal>shade</goal>
            </goals>
            <configuration>
              <transformers>
                <transformer>
                  <mainClass>${main.class}</mainClass>
                </transformer>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <profiles>
    <profile>
      <id>package-exe</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.panteleyev</groupId>
            <artifactId>jpackage-maven-plugin</artifactId>
            <version>1.4.0</version>
            <executions>
              <execution>
                <id>win</id>
                <phase>package</phase>
                <goals>
                  <goal>jpackage</goal>
                </goals>
              </execution>
            </executions>
            <configuration>
              <name>${app.name}</name>
              <appVersion>${project.version}</appVersion>
              <type>APP_IMAGE</type>
              <mainClass>${main.class}</mainClass>
              <mainJar>${project.build.finalName}.jar</mainJar>
              <input>${project.build.directory}</input>
              <destination>target/dist</destination>
              <javaOptions>
                <option>-Dfile.encoding=UTF-8</option>
              </javaOptions>
              <arguments>
                <argument>--win-console</argument>
                <argument>--win-menu</argument>
                <argument>--win-menu-group</argument>
                <argument>Digimon Simulator</argument>
                <argument>--win-shortcut</argument>
                <argument>--type</argument>
                <argument>msi</argument>
              </arguments>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
  <properties>
    <maven.compiler.target>17</maven.compiler.target>
    <main.class>com.horrorcore.DigimonSimulator</main.class>
    <maven.compiler.source>17</maven.compiler.source>
    <javafx.maven.plugin.version>0.0.8</javafx.maven.plugin.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <javafx.version>17.0.6</javafx.version>
    <app.name>DigimonSimulator</app.name>
  </properties>
</project>
